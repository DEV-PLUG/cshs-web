import hansearch from "hangul-search";

export default function handleSarch(array:any[], search:string) {
  // return hansearch(array, search).items;
}

// 자체 라이브러리 제작 권장.
// Debound 및 초성 분리 등의 기능 구현 필요.